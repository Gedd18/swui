<local:CommandPanel x:Class="SWUI.SVGTextButton"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SWUI"
             xmlns:si="clr-namespace:SVGImage.SVG;assembly=DotNetProjects.SVGImage"
             mc:Ignorable="d" d:DesignHeight="450" d:DesignWidth="800" Loaded="CommandPanel_Loaded"
             Background="Transparent" x:Name="me">
    <StackPanel>
        <DockPanel LastChildFill="true">
            <TextBlock FontWeight="Bold" VerticalAlignment="Center" Text="{Binding Text, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type local:SVGTextButton}}}"/>
            <si:SVGImage Width="32" Height="32" x:Name="img" DockPanel.Dock="Right"/>
        </DockPanel>
    </StackPanel>
</local:CommandPanel>
